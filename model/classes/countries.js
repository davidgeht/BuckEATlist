class Country{
    constructor(id, name, currency, created_at){
        this.id=id,
        this.name=name,
        this.currency=currency,
        this.created_at=created_at
    }
}

module.exports=Country;